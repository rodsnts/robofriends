import React, { Fragment } from 'react';

const SearchBox = () => {
    return (
        <Fragment>
            <input type='search' placeholder='Search a Robot...' />
        </Fragment>
    );
}

export default SearchBox;